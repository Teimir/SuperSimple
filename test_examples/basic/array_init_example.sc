// Example: Array initialization
function main() {
    // Initialize array with values
    uint32 arr[5] = {10, 20, 30, 40, 50};
    
    // Access initialized values
    uint32 sum = arr[0] + arr[1] + arr[2] + arr[3] + arr[4];
    
    return sum;  // Returns 150 (10+20+30+40+50)
}
